# Twilio
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=+15551234567

# number the bot will call to test the target voice bot
TARGET_TEST_NUMBER=+15559876543

# OpenAI
OPENAI_API_KEY=
OPENAI_REALTIME_MODEL=gpt-realtime-mini

# publicly reachable base URL for this server (ngrok URL during dev,
# or your deployed host) - used to build the TwiML webhook + stream URLs
PUBLIC_BASE_URL=https://your-ngrok-subdomain.ngrok-free.app
